  <div class="scrollbar-inner">

    <div class="user">
      <div class="user__info" data-toggle="dropdown">

        <%= image_tag('default_avatar.png', :class => 'user__img', alt: '')%>

        <div>
          <div class="user__name"><%= current_user.name %></div>
          <div class="user__email"> <%= current_user.email %> </div>
        </div>
      </div>

      <div class="dropdown-menu">
        <!--<a class="dropdown-item" href="#">View Profile</a>-->
        <%= link_to('Settings', edit_user_registration_path, class: 'dropdown-item')%>
        <%= link_to('Logout', destroy_user_session_path, class: 'dropdown-item')%>
      </div>
    </div>

    <ul class="navigation">
      <li>
          <%= link_to root_path do %>
              <i class="zmdi zmdi-home"></i> Inicio
          <% end %>
    </ul>
  </div>
